from .models import db, UserGameData
import json

def get_game_data(session_id, game_name, data_type, default=None):
    # [Implementation as previously discussed]
    pass

def save_game_data(session_id, game_name, data_type, data_dict):
    # [Implementation as previously discussed]
    pass
